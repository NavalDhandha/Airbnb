snapshots:
    - name: dim_hosts
      relation: ref('hosts') 
      config:
        schema: gold
        database: AIRBNB
        unique_key: host_id
        strategy: timestamp
        updated_at: hosts_created
        dbt_valid_to_current: "to_date('9999-01-01')"