snapshots:
    - name: dim_listings
      relation: ref('listings') 
      config:
        schema: gold
        database: AIRBNB
        unique_key: listing_id
        strategy: timestamp
        updated_at: listings_created
        dbt_valid_to_current: "to_date('9999-01-01')"